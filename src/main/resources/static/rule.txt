数据库命名：小写，下划线连接，
后台命名：驼峰式，禁止使用下划线连接
前端命名：驼峰式，禁止使用下划线连接

数据库date类型，查询时（select）不做date_format转换，作为where判断条件时要做date_format转换
查询单条记录（对象）时，也用list返回

加载回调，关闭加载动画，不判断成功失败
事件触发回调，show message，判断成功失败，成功执行xxx，失败执行xxx